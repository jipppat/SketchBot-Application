name: sketchbot_simulator
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_plugin_android_lifecycle: ^2.0.6
  image_picker: ^1.2.0
  shared_preferences: ^2.1.1
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  path_provider: ^2.1.4
  uuid: ^4.5.0
  flutter_painter_v2: ^2.1.0+1
  image: ^4.2.0
  roslibdart: 0.0.1-dev+4

  # ✅ Firebase Gen 2 (compatible with existing code)
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.17.5
  firebase_storage: ^11.7.7
  firebase_app_check: ^0.2.2+2
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/newlogo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/
